// Qual o resultado da seguinte expressão?
var total = 10 + (5 * 2) / 2 + 20;
console.log(total); // 25

// Crie duas expressões que retornem NaN
var expressao1 = "Olá" / 2; // NaN
var expressao2 = "Texto" - 5; // NaN

// Somar a string '200' com o número 50 e retornar 250
var resultado = "200" + 50;
console.log(resultado); // '20050'

// Incremente o número 5 e retorne o seu valor incrementado
var numero = 5;
var incrementado = ++numero;
console.log(incrementado); // 6

// Como dividir o peso por 2?
var numero = "80";
var unidade = "kg";
var peso = parseFloat(numero); // Convertendo a string para número
var pesoPorDois = peso / 2;
console.log(pesoPorDois); // 40
